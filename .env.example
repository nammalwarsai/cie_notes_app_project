# Environment Variables Example File
# Copy this file to create your .env files for backend and frontend

# ==========================================
# BACKEND .env (backend/.env)
# ==========================================

# Server Configuration
PORT=3000
NODE_ENV=production

# AWS Configuration
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here

# DynamoDB Configuration
DYNAMODB_TABLE_NAME=NotesAppTable
DYNAMODB_ENDPOINT=https://dynamodb.us-east-1.amazonaws.com

# CORS Configuration (Frontend URL)
FRONTEND_URL=http://YOUR_EC2_PUBLIC_IP:3001

# JWT Secret (Generate a strong random secret)
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Session Secret (Generate a strong random secret)
SESSION_SECRET=your_session_secret_key_change_this_too

# ==========================================
# FRONTEND .env (frontend/.env)
# ==========================================

# Backend API URL
REACT_APP_API_URL=http://YOUR_EC2_PUBLIC_IP:3000

# Environment
NODE_ENV=production

# ==========================================
# NOTES:
# ==========================================
# 
# 1. NEVER commit .env files to GitHub
# 2. Replace YOUR_EC2_PUBLIC_IP with actual EC2 public IP
# 3. Generate strong secrets using:
#    node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# 4. AWS credentials need DynamoDB read/write permissions
# 5. Keep these files secure and never share them
